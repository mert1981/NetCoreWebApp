@model List<KitapTuru> 

@if(TempData["basarili"] != null ){

    <h2 style="color:yellowgreen">@TempData["basarili"] </h2>
}

<div class="container">

    <div class="row pt-3 pb-4">
        <div>
            <h1>Kitap Türleri</h1>
        </div>
    </div>

        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Kitap Türü</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @foreach (var kitapturu in Model)
                {
                    <tr>
                        <td>@kitapturu.Id</td>
                        <td>@kitapturu.Ad</td>
                        <td>
                            <a asp-controller="KitapTuru" asp-action="Guncelle" asp-route-id="@kitapturu.Id" type="button" class="btn btn-success" style="width:200px" >Güncelle</a>
                        </td>
                        <td>
                            <a asp-controller="KitapTuru" asp-action="Sil" asp-route-id="@kitapturu.Id" type="button" class="btn btn-danger" style="width:200px">Sil</a>
                        </td>
                    </tr>
                }
        
            </tbody>
        </table>

        <div class="row pt-3 pb-2">
            <div>
                <a asp-controller="KitapTuru" asp-action="Ekle" class="btn btn-lg btn-primary" type="button">Kitap Türü Ekle 

                </a>
            </div>
        </div>
</div>